using System.Collections;
using System.Collections.Generic;
using UnityEngine;

    
public class Player : MonoBehaviour
{
    //PlayerMovent
    // Start is called before the first frame update
    /*
     * 1ro les voy a hablar sobre los input del juego 
     * que son y donde se configuran. pata que despues ellos lo hagan a su gusto. 
     * despues explico que como este juego lo voy a hacer para mobile, ademas de bajarle los fame por segundos 
     para mover el player lo que tenemos que hacer es 

     */
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
